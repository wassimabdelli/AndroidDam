<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CRITICAL FIX: Add INTERNET permission here, outside the <application> block -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/stade3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/stade3"
        android:supportsRtl="true"
        android:theme="@style/Theme.DAM">

        <!-- Welcome Screen Activity (No need to export as false) -->
        <activity
            android:name=".activity.WelcomeScreenActivity"
            android:exported="true" />  <!-- Set to true if necessary -->

        <!-- Main Activity is your entry point now -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>